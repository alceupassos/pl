.DS_Store
.env.local
.omx/
dist/
node_modules/
.next/
out/
.env

# apresentação gerada
*.pptx

# pasta aninhada não relacionada (M&A/BPO)
/sostenes/

# contador de build do carimbo de versão (persiste no servidor, fora do git)
/data/build-counter.json

# cache dos índices reais (GDELT) — runtime, persiste no servidor
/data/gdelt-cache.json

# cache do índice de imprensa (Google News) — runtime, persiste no servidor
/data/google-news-cache.json

# cache da cota parlamentar real (Câmara) — runtime, persiste no servidor
/data/camara-cache.json
