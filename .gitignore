.DS_Store
.env.local
.omx/
dist/
node_modules/
.next/
out/
.env

# apresentação gerada
*.pptx

# pasta aninhada não relacionada (M&A/BPO)
/sostenes/

# contador de build do carimbo de versão (persiste no servidor, fora do git)
/data/build-counter.json

# cache dos índices reais (GDELT) — runtime, persiste no servidor
/data/gdelt-cache.json

# cache do índice de imprensa (Google News) — runtime, persiste no servidor
/data/google-news-cache.json

# cache da cota parlamentar real (Câmara) — runtime, persiste no servidor
/data/camara-cache.json

# venv do sidecar de sentimento (instalado no servidor, fora do git)
/sidecar/sentiment/.venv/
/sidecar/sentiment/__pycache__/
/sidecar/sentiment/brightdata_snapshots.json

# cache de inscritos do YouTube (yt-dlp) — runtime, persiste no servidor
/data/youtube-cache.json
/data/youtube-profiles-cache.json

# cache de pesquisas reais (Wikipédia via sidecar) — runtime, persiste no servidor
/data/pesquisas-cache.json

# caches das fontes reais T1–T3 (composer.md) — runtime, persiste no servidor
/data/plenario-cache.json
/data/trends-cache.json
/data/youtube-videos-cache.json
/data/idx-history-cache.json
# histórico por candidato do Score+IRE (tendência 7d) e dos seguidores — runtime
/data/index-history-cache.json
/data/seguidores-history.json
/data/instagram-cache.json
/data/facebook-cache.json
/data/x-cache.json

# briefings colaborativos do /basecalculo — runtime, persiste no servidor
/data/briefings.jsonl
